import 'package:flutter/material.dart';

class JoinActiveStream extends StatelessWidget {
  const JoinActiveStream({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}